# 알리미 앱

**https://clddy.github.io/trash-map/app/**

폰에서 열어 **⋮ → 홈 화면에 추가**(안드로이드) / **공유 → 홈 화면에 추가**(아이폰)를 누르면
앱 아이콘이 생기고, 주소창 없이 앱처럼 뜬다.

---

## 왜 Flutter가 아닌가

계획서는 Flutter를 적어뒀지만, 지금 이 단계에서는 맞지 않는다.

| | Flutter 앱 | 지금 만든 것 |
|---|---|---|
| 시작 비용 | Flutter SDK + 안드로이드 SDK **약 7GB** 설치 | 0 |
| 확인 | 에뮬레이터/실기기 필요 | **폰에서 링크만 열면 됨** |
| 홈 위젯 | **어차피 안드로이드 네이티브로 따로 짜야 함** | 불가 |
| 배포 | 스토어 심사 | 즉시 |

**홈 위젯이 이 앱의 차별점인데, 위젯은 Flutter로 못 만든다.** 안드로이드 위젯은
RemoteViews/Glance로 네이티브 작성이 필수라, Flutter를 써도 가장 중요한 부분은 이중 작업이 된다.

그래서 순서를 바꿨다. **로직을 먼저 확정하고**, 위젯이 필요해지는 시점에 네이티브로 간다.
`engine.js`가 그때 정답지가 된다.

### 지금 것으로 되는 것 / 안 되는 것

- ✅ **동네 이름 검색 한 번으로 설정 완료** (전국 1,189구역)
- ✅ 오늘 뭐 버리는지, 주간표, 오프라인 동작, 홈 화면 설치
- ✅ 알림 권한 + 발송, 백그라운드로 깨워주면 그때 발송
- ❌ **홈 화면 위젯** — 네이티브 전용
- ❌ **정확한 시각 알림** — 웹은 '매일 저녁 7시'를 보장하지 못한다.
  브라우저가 깨우는 시점을 정하기 때문이다.

마지막 두 개가 제품의 핵심이라 **결국 네이티브로 가야 한다.**
다만 그건 로직이 굳은 뒤에 할 일이고, `engine.js`가 그때 정답지가 된다.

---

## 구조

```
app/
  index.html    화면 틀 + 스타일
  app.js        4개 탭(오늘·알림·정보·설정), localStorage 저장
  engine.js     ★ 알림 엔진 — simulate_alerts.py 규칙을 그대로 이식
  data.js       66개 지역 · 1,189구역 · 3,497규칙 · 공휴일 (1.1MB)
  sw.js         오프라인 캐시 (네트워크 우선, 실패 시 캐시)
  manifest.json 홈 화면 설치용
```

### engine.js — 여기가 핵심

파이썬 시뮬레이터에서 지자체를 붙일 때마다 버그가 나왔고, 그때 정한 규칙이 다 들어 있다.

| 규칙 | 왜 생겼나 |
|---|---|
| **희소도 필터** | 관악은 토요일 빼고 매일 배출 가능인데 "오늘 버리세요"가 매일 떴다. 주 6~7일 품목은 당일 알림을 끈다 |
| **solo 판정** | 의정부는 금요일에 음식물만 가능한데, 주 6일이라 희소도로 안 잡혔다. 다른 품목이 전부 쉬는 날엔 알린다 |
| **solo 남발 방지** | 강남은 그 외 재활용이 주 5일 전부 solo라 "OO만 버릴 수 있어요"가 매일 떴다. 주 3일 이상이면 평상시로 본다 |
| **연휴 예고** | 광진구는 광복절 주간에 금·토·일 3일 연속 금지. "오늘 버리세요"보다 "앞으로 3일 못 버립니다"가 행동을 바꾼다 |
| **평소 간격 비교** | 김포 주1회 마을에서 매주 "6일간 금지"가 떴다. 평소보다 길어질 때만 예고한다 |
| **중복 제거** | 양평은 월·화·목이라 월요일에 "오늘"과 "내일"이 동시에 떴다. 당일이 전날 예고를 덮는다 |

---

## 데이터 정직성

앱이 **모르는 걸 아는 척하지 않는다.**

- 요일 데이터가 없는 항목은 붉은 카드로 **"데이터 없음 + 사유 + 문의처"**
- 파주·남양주처럼 시가 요일을 안 가진 곳은 **담당 수거업체 원터치 전화**
- 확신도가 낮은 값(포천, 서울 파생 규칙)은 **"확인이 필요한 값"으로 별도 표시**
- 음력 공휴일(설·추석)은 미검증이라 **그 기간 알림을 보내지 않는다**

배출요일을 틀리면 사용자가 과태료를 물고 앱 신뢰는 즉사한다. 빈칸을 남기는 게 낫다.

---

## 빌드

```bash
python tools/build_all.py
```

데이터 생성 → **검증** → 산출물 순서다. 검증이 실패하면 산출물을 만들지 않는다.
`validate_regions.py`가 잡는 것들(zone_id 중복 등)은 조용히 배포되면 사고가 난다.

배포:
```bash
cd site && git add -A && git commit -m "..." && git push
```

---

## 온보딩 — 검색이 기본

시·도 → 시·군·구 → 동네 3단계 드롭다운이 이 앱에서 가장 번거로운 지점이었다.
김포는 마을이 410개, 안성은 549개라 목록을 훑는 게 현실적이지 않다.

그래서 **동네 이름만 치면 전국에서 찾는다.** 주소검색 API 없이 기존 데이터만으로 된다.

```
"주안8동"  → 인천광역시 미추홀구 · 음식물 일화목 / 주안8동 · 관교동 · 문학동
"정릉4동"  → 서울특별시 성북구 · 정릉 / 정릉4동
"대룡리"   → 인천광역시 강화군 · 교동면 / 대룡리
"청라"     → 인천광역시 서구 · 재활용만 일·화·목 / 청라동 (상가 및 단독주택)
```

- 요일 데이터가 있는 곳을 먼저, 짧은 라벨(정확한 매칭) 순으로 정렬
- 결과에 시·도/시·군·구와 **"요일 데이터 없음"** 여부를 같이 표시
- 검색 → 선택 → 저장 → 오늘 화면까지 **한 번에**
- 드롭다운은 못 찾을 때의 대안으로 아래에 남겨둠

주소검색 API를 붙이면 여기서 한 단계 더 줄지만(`ADDRESS_DESIGN.md`),
**API 없이도 실용적인 수준까지는 왔다.**

---

## 다음

1. **알림 스케줄링** — 웹에서 할 수 있는 만큼은 붙였다.
   앱을 열 때마다 오늘치 알림을 서비스워커에 넘겨두고, 브라우저가 백그라운드로
   깨워주면(`periodicsync`) 그때 발송한다. 확신도 낮은 알림은 제외한다.
   **다만 '매일 저녁 7시'는 웹이 보장하지 못한다.** 브라우저가 시점을 정하기 때문이다.
   정확한 시각 알림은 안드로이드 네이티브(WorkManager + 로컬 알림)가 필요하다.
   앱의 알림 탭에도 이 한계를 그대로 적어뒀다 — "알림 켰는데 안 온다"가 최악이라서.
2. **홈 위젯** — 계획서가 차별점으로 꼽은 것. 네이티브 전용.
3. **주소 검색** — 지금은 시·군·구를 직접 고른다.
   [주소검색 API](https://business.juso.go.kr/jst/jstRoadNmAddrApiSearch)를 붙이면
   질문이 3개에서 0~1개로 준다 (`ADDRESS_DESIGN.md`). **API 신청은 계정이 필요하다.**
